# Day 3 - Treasure Island Game

This day focused on **if / elif / else**, **nested conditionals**, and **user input handling** in Python.

## Overview
- A text-based adventure game where you make choices to find the treasure.
- Inputs are case-insensitive (`left` = `Left` = `LEFT`).

## How to Play
1. Run the Python file in your terminal:

```bash
python day3.py
